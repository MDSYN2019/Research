#ifndef _QUADTREE_
#define _QUADTREE_

/*
The conditional operator evaluates an expression, returning one value if that expression evaluates to true, and a different 
one if the expression evaluates as false.
 */

#include <vector>
#include <iostream>

struct Point {
  float x, y;
  Point(float x = 0, float y = 0): x(x) , y(y) {};
}

  struct AABB {
  }
  

#endif
