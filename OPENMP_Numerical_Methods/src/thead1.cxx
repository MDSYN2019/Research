#include <iostream>
#include <thread>

/*
Concurrency in computers - we mean a single system performing independent activities in parallel, rather than sequentially one after the other.

 */

int main () {
  return 0;
  
}
